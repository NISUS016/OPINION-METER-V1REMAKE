# Setup Log - OpinionMeter V1 Remake

1. **Dataset Preparation:**
   - Created directory `OPINION-METER-V1REMAKE/flipkart-dataset`.
   - Moved dataset file from `OPINION-METER-V1REMAKE/archive/Dataset-SA.csv` to `OPINION-METER-V1REMAKE/flipkart-dataset/Dataset-SA.csv` as per design specifications.

2. **Dependency Installation:**
   - Installed core Python dependencies for training and backend: `pandas`, `scikit-learn`, `nltk`, `joblib`, `fastapi`, `uvicorn[standard]`, `python-multipart`.
   - Successfully installed all required Node.js dependencies for the initial and new frontend.

3. **Model Training:**
   - Executed `python train_model.py`.
   - Preprocessed the Flipkart dataset (~205,000 reviews).
   - Trained three models: Logistic Regression, Naive Bayes, and Linear SVM.
   - Selected the **Linear SVM** model (highest accuracy: 0.9110).
   - Saved the trained model, TF-IDF vectorizer, and label encoder to `backend/models/`.

4. **Frontend Replacement:**
   - Dumped the old frontend to `frontend_old`.
   - Replaced `frontend` with the contents of `frontend-2`.
   - Updated `App.tsx` and `vite.config.ts` to connect to the backend API seamlessly.
   - New frontend features a "Precision Intelligence" design with a landing, loading, and dashboard view.

5. **System Startup:**
   - Backend: `uvicorn main:app --port 8000` (Running)
   - Frontend: `npm run dev` (Running on port 3000)

**Current Status:**
- Backend: http://localhost:8000
- Frontend: http://localhost:3000

6. **Bug Fixes & Dataset Optimization:**
   - Fixed a backend Internal Server Error (500) caused by a missing 'Rate' column in the pandas `usecols` parameter.
   - Updated the data pipeline to use the 'Summary' column for the primary review text, providing more detailed, authentic feedback compared to the short 'Review' labels.
   - Integrated 'short_label' (original Review column) into the UI for additional context (e.g., "super!", "awesome").

7. **UI Refinements:**
   - Implemented `truncateWords` utility to cap reviews at 100 words with a "..." suffix.
   - Reduced review font sizes and improved padding for a cleaner "Individual Evidence" layout.
   - Removed unnecessary UI elements (notifications, settings, profile icons, analytics link) to streamline the dashboard.
   - Removed the "AI Prediction" label from the sentiment display to reduce visual clutter.

8. **New Features:**
   - Implemented a persistent Session History system.
   - Added a dedicated "History" view that allows users to revisit and re-analyze previously searched products.
   - Clicking "Opinion-Meter" logo now correctly navigates back to the landing page.

9. **V2 Architecture & Feature Set (Completed):**
   - **Codebase Refactoring:** Successfully split the monolithic `App.tsx` into a modular architecture using dedicated components (`LandingView`, `LoadingView`, `DashboardView`, `HistoryView`, `ReviewCard`, `Footer`).
   - **Backend Caching:** Implemented in-memory caching for both search and analysis endpoints to provide near-instantaneous results for repeated queries.
   - **Dark Mode Removal:** Removed dark mode functionality and associated styling to maintain a consistent, professional light theme.
   - **Review Filtering & Sorting:** Integrated dynamic filtering (Positive/Neutral/Negative) and sorting (Highest/Lowest Rating) in the dashboard.
   - **Enhanced Data Visualization:** Replaced the standard bar chart with a high-impact Donut chart for sentiment composition.
   - **Data Export:** Added "Export CSV" functionality to download analysis reports directly from the dashboard.
   - **UI/UX Polishing:** Improved responsiveness, added dark-mode themed scrollbars, and refined typography.

**Final Status:** 
The application is fully optimized, feature-rich, and ready for production-level analysis.
- Backend: http://localhost:8000 (with Caching)
- Frontend: http://localhost:3000 (Modular, Dark Mode, Filtering)
